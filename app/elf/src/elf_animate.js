/*
 * Created by lijianwei on 2017/11/1.
 */

/*
 * target:执行动画的元素
 * type:动画类型，动画库定义的动画类型
 * options:参数配置列表
 * 		
 * callback:动画执行完成回调
 */
Elf.animate=function(target,type,options,callback){
	
};
Elf.fadein=function(target,options,callback){
	
};
Elf.fadeout=function(target,options,callback){
	
};